# Dependencies
node_modules/

# Build artefact (the control-plane image builds ui/dist in CI)
dist/

# The router plugin regenerates this on every build; not source of truth
# (kept out of git so a stale tree never fights the generator).
src/routeTree.gen.ts

# Vite / editor cruft
.vite/
*.local
.DS_Store
