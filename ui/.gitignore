# Dependencies
node_modules/

# Build artefact (the control-plane image builds ui/dist in CI)
dist/

# NOTE: src/routeTree.gen.ts IS committed. `npm run build` runs tsc before the
# vite plugin regenerates the tree, so tsc needs it present in a fresh checkout
# (CI). The plugin overwrites it on every build/dev, so it stays in sync.

# Vite / editor cruft
.vite/
*.local
.DS_Store
