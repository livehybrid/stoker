# Splunk's front-end packages are published against yarn's looser peer
# resolution, and npm 7+ refuses some of their declared peer ranges outright.
# Setting this here rather than relying on everyone remembering the flag is what
# keeps `npm ci` in CI and `npm install` on a laptop producing the same tree.
legacy-peer-deps=true
