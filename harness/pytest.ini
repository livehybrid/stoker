[pytest]
# Standalone: this harness drives a LIVE deployment and is not part of the unit
# CI (server/tests, worker/tests, tools/tests). Run it explicitly: `pytest harness`.
testpaths = .
addopts = -ra -q
