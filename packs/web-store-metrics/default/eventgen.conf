# Stub for a metrics pack (engine=metrics). The metrics engine reads its config
# from stoker.json's `metricgen` block and the agent skips the conf-rewrite; this
# file exists only to satisfy the worker's pack-root file contract (fetch_bundle
# requires default/eventgen.conf) and is never executed.
[stub]
mode = sample
