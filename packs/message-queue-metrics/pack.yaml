# Stoker metric pack: the metrics engine reads its config from
# stoker.json's `metricgen` block. Runs on the `metrics` engine;
# deliver to a metrics-type index. See docs/PACKS.md.
name: message-queue-metrics
tags: metrics, messaging, kafka, queue, ops
engine: metrics
description: "Message-queue/streaming metrics (publish/consume rates, consumer lag, queue depth) across topic x consumer_group, business-hours shaped with lag bursts."
estimates:
  bytes_per_event: 300
