# Stub for a metrics pack (engine=metrics). The metrics engine reads its
# config from stoker.json's `metricgen` block and the agent skips the
# conf-rewrite; this file only satisfies the pack-root file contract
# (fetch_bundle requires default/eventgen.conf) and is never executed.
[stub]
mode = sample
